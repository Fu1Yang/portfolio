{% extends 'base.html.twig' %}

{% block title %}MesProjet{% endblock %}

{% block body %}
    <h1>MesProjet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mes_projet.id }}</td>
            </tr>
            <tr>
                <th>Images</th>
                <td>{{ mes_projet.images }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ mes_projet.titre }}</td>
            </tr>
            <tr>
                <th>Paragraphe</th>
                <td>{{ mes_projet.paragraphe }}</td>
            </tr>
            <tr>
                <th>DiagrammeMcd</th>
                <td>{{ mes_projet.diagrammeMcd }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mes_projet_index') }}">back to list</a>

    <a href="{{ path('app_mes_projet_edit', {'id': mes_projet.id}) }}">edit</a>

    {{ include('mes_projet/_delete_form.html.twig') }}
{% endblock %}
