<form method="post" action="{{ path('app_mes_projet_delete', {'id': mes_projet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mes_projet.id) }}">
    <button class="btn">Delete</button>
</form>
